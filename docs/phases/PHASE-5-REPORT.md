# PHASE 5 REPORT — Supply Chain (SBOM, Signing, Verification)

Status: **PARTIAL** — SBOM + signing + verification DONE; Kyverno `verify-images` admission policy pending (Phase 8).

## Done
- CycloneDX SBOMs per image (Syft), generated by Jenkins and archived.
- Key-based cosign keypair; public key committed at `security/cosign.pub`; private key outside Git.
- Images signed **by digest** in Jenkins; in-pipeline `cosign verify` passes.
- Local verification of the deployed digest passes ("signatures were verified against the specified public key").
- All four images: Trivy HIGH/CRITICAL = 0 after base-image and dependency remediation.

## Evidence
- Jenkins build #12 (sign + verify stages) SUCCESS.
- `security/sbom/*.cdx.json` (generated); `docs/05-supply-chain.md`.

## Remaining
- Kyverno `verify-images` ClusterPolicy (namespace-scoped to `eshtry-mny`): Audit -> Enforce, prove unsigned image rejection, then confirm the four existing chart policies are unaffected.
